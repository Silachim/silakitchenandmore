// Sila's Kitchen and More — Batch 3B configuration
// Replace ONLY the two placeholders below.
// The publishable/anon key is safe to expose in a browser when RLS is configured.
// NEVER place a Supabase service-role key in this file.

window.SILA_SUPABASE = {
  url: "https://YOUR-PROJECT-REF.supabase.co",
  anonKey: "YOUR_SUPABASE_PUBLISHABLE_OR_ANON_KEY",
  receiptBucket: "payment-receipts"
};
